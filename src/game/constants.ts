/** FLOW CONNECT identity and the handful of numbers everything else derives from. */

export const GAME_NAME = "FLOW CONNECT";
export const GAME_TAGLINE = "NEON LOGIC PUZZLES";

/** Smallest and largest board edge the scene is laid out for. */
export const MIN_BOARD_SIZE = 5;
export const MAX_BOARD_SIZE = 9;

/** Most flows any board may have — one tube colour each. */
export const MAX_FLOWS = 10;

export type QualityPreset = "high" | "low";
