{
  "name": "flow-connect",
  "version": "1.1.0",
  "description": "FLOW CONNECT — a portrait neon logic puzzle for RUN.world: connect matching colours and fill every cell. Built with PixiJS 8 (WebGPU-first).",
  "private": true,
  "license": "SEE LICENSE IN LICENSE.md",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "allowScripts": {
    "@firebase/util": false,
    "esbuild@0.25.12": true,
    "fsevents": false,
    "protobufjs": false,
    "@series-inc/rundot-game-sdk@5.28.1": true
  },
  "scripts": {
    "version:check": "node scripts/check-deployed-version.mjs",
    "dev": "vite",
    "dev:playground": "cross-env RUNDOT_PLAYGROUND=1 vite",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint src scripts vite.config.js --diagnostic-level=error --max-diagnostics=none",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test-noise-random.mjs && node scripts/check-safe-area.ts && node scripts/simulate.mjs && node scripts/check-game.mjs",
    "simulate": "node scripts/simulate.mjs",
    "balance": "node scripts/simulate.mjs --sweep",
    "thumbnail": "node scripts/make-thumbnail.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs",
    "visual-qa": "node scripts/visual-qa.mjs",
    "audit:public": "node scripts/audit-public.mjs",
    "build": "npm run typecheck && vite build && node scripts/check-build.mjs embedded",
    "build:bundled": "npm run typecheck && cross-env RUNDOT_GAME_DISABLE_EMBEDDED_LIBS=true vite build && node scripts/check-build.mjs bundled",
    "check": "npm run format:check && npm run lint && npm run test && npm run audit:public && npm run build && npm run build:bundled",
    "preview": "vite preview",
    "levels": "node scripts/build-levels.mjs"
  },
  "dependencies": {
    "@series-inc/rundot-game-sdk": "5.28.1",
    "pixi.js": "^8.18.1",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@revenuecat/purchases-js": "^1.8.0",
    "@tailwindcss/vite": "^4.1.5",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.1",
    "cross-env": "^7.0.3",
    "firebase": "^12.19.0",
    "playwright-core": "^1.62.0",
    "tailwindcss": "^4.1.5",
    "typescript": "^5.8.0",
    "vite": "^6.3.5"
  }
}
